// <auto-generated>
// THIS (.cs) FILE IS GENERATED BY T4. DO NOT CHANGE IT. CHANGE THE .tt FILE INSTEAD.
// </auto-generated>
using System;
using System.Collections.Generic;
using System.Numerics;
using System.Runtime.CompilerServices;
using System.Text;
using NVector = System.Numerics.Vector;

namespace SmartVectorDotNet;

partial class VectorCompatible
{

    public static partial Vector<byte  > ShiftLeft(in Vector<byte  > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftLeft(value, shiftCount);
#else
        switch(Vector<byte>.Count)
        {
        case 8:
            return new(stackalloc []{
                (byte  )(value[0] << shiftCount),
                (byte  )(value[1] << shiftCount),
                (byte  )(value[2] << shiftCount),
                (byte  )(value[3] << shiftCount),
                (byte  )(value[4] << shiftCount),
                (byte  )(value[5] << shiftCount),
                (byte  )(value[6] << shiftCount),
                (byte  )(value[7] << shiftCount),
            });
        case 16:
            return new(stackalloc []{
                (byte  )(value[0] << shiftCount),
                (byte  )(value[1] << shiftCount),
                (byte  )(value[2] << shiftCount),
                (byte  )(value[3] << shiftCount),
                (byte  )(value[4] << shiftCount),
                (byte  )(value[5] << shiftCount),
                (byte  )(value[6] << shiftCount),
                (byte  )(value[7] << shiftCount),
                (byte  )(value[8] << shiftCount),
                (byte  )(value[9] << shiftCount),
                (byte  )(value[10] << shiftCount),
                (byte  )(value[11] << shiftCount),
                (byte  )(value[12] << shiftCount),
                (byte  )(value[13] << shiftCount),
                (byte  )(value[14] << shiftCount),
                (byte  )(value[15] << shiftCount),
            });
        case 32:
            return new(stackalloc []{
                (byte  )(value[0] << shiftCount),
                (byte  )(value[1] << shiftCount),
                (byte  )(value[2] << shiftCount),
                (byte  )(value[3] << shiftCount),
                (byte  )(value[4] << shiftCount),
                (byte  )(value[5] << shiftCount),
                (byte  )(value[6] << shiftCount),
                (byte  )(value[7] << shiftCount),
                (byte  )(value[8] << shiftCount),
                (byte  )(value[9] << shiftCount),
                (byte  )(value[10] << shiftCount),
                (byte  )(value[11] << shiftCount),
                (byte  )(value[12] << shiftCount),
                (byte  )(value[13] << shiftCount),
                (byte  )(value[14] << shiftCount),
                (byte  )(value[15] << shiftCount),
                (byte  )(value[16] << shiftCount),
                (byte  )(value[17] << shiftCount),
                (byte  )(value[18] << shiftCount),
                (byte  )(value[19] << shiftCount),
                (byte  )(value[20] << shiftCount),
                (byte  )(value[21] << shiftCount),
                (byte  )(value[22] << shiftCount),
                (byte  )(value[23] << shiftCount),
                (byte  )(value[24] << shiftCount),
                (byte  )(value[25] << shiftCount),
                (byte  )(value[26] << shiftCount),
                (byte  )(value[27] << shiftCount),
                (byte  )(value[28] << shiftCount),
                (byte  )(value[29] << shiftCount),
                (byte  )(value[30] << shiftCount),
                (byte  )(value[31] << shiftCount),
            });
        default:
            {
                var buffer = (stackalloc byte[Vector<byte>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = (byte  )(value[i] << shiftCount);
                }
                return Unsafe.As<byte, Vector<byte>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<ushort> ShiftLeft(in Vector<ushort> value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftLeft(value, shiftCount);
#else
        switch(Vector<ushort>.Count)
        {
        case 4:
            return new(stackalloc []{
                (ushort)(value[0] << shiftCount),
                (ushort)(value[1] << shiftCount),
                (ushort)(value[2] << shiftCount),
                (ushort)(value[3] << shiftCount),
            });
        case 8:
            return new(stackalloc []{
                (ushort)(value[0] << shiftCount),
                (ushort)(value[1] << shiftCount),
                (ushort)(value[2] << shiftCount),
                (ushort)(value[3] << shiftCount),
                (ushort)(value[4] << shiftCount),
                (ushort)(value[5] << shiftCount),
                (ushort)(value[6] << shiftCount),
                (ushort)(value[7] << shiftCount),
            });
        case 16:
            return new(stackalloc []{
                (ushort)(value[0] << shiftCount),
                (ushort)(value[1] << shiftCount),
                (ushort)(value[2] << shiftCount),
                (ushort)(value[3] << shiftCount),
                (ushort)(value[4] << shiftCount),
                (ushort)(value[5] << shiftCount),
                (ushort)(value[6] << shiftCount),
                (ushort)(value[7] << shiftCount),
                (ushort)(value[8] << shiftCount),
                (ushort)(value[9] << shiftCount),
                (ushort)(value[10] << shiftCount),
                (ushort)(value[11] << shiftCount),
                (ushort)(value[12] << shiftCount),
                (ushort)(value[13] << shiftCount),
                (ushort)(value[14] << shiftCount),
                (ushort)(value[15] << shiftCount),
            });
        default:
            {
                var buffer = (stackalloc ushort[Vector<ushort>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = (ushort)(value[i] << shiftCount);
                }
                return Unsafe.As<ushort, Vector<ushort>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<uint  > ShiftLeft(in Vector<uint  > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftLeft(value, shiftCount);
#else
        switch(Vector<uint>.Count)
        {
        case 2:
            return new(stackalloc []{
                value[0] << shiftCount,
                value[1] << shiftCount,
            });
        case 4:
            return new(stackalloc []{
                value[0] << shiftCount,
                value[1] << shiftCount,
                value[2] << shiftCount,
                value[3] << shiftCount,
            });
        case 8:
            return new(stackalloc []{
                value[0] << shiftCount,
                value[1] << shiftCount,
                value[2] << shiftCount,
                value[3] << shiftCount,
                value[4] << shiftCount,
                value[5] << shiftCount,
                value[6] << shiftCount,
                value[7] << shiftCount,
            });
        default:
            {
                var buffer = (stackalloc uint[Vector<uint>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = value[i] << shiftCount;
                }
                return Unsafe.As<uint, Vector<uint>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<ulong > ShiftLeft(in Vector<ulong > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftLeft(value, shiftCount);
#else
        switch(Vector<ulong>.Count)
        {
        case 1:
            return new(stackalloc []{
                value[0] << shiftCount,
            });
        case 2:
            return new(stackalloc []{
                value[0] << shiftCount,
                value[1] << shiftCount,
            });
        case 4:
            return new(stackalloc []{
                value[0] << shiftCount,
                value[1] << shiftCount,
                value[2] << shiftCount,
                value[3] << shiftCount,
            });
        default:
            {
                var buffer = (stackalloc ulong[Vector<ulong>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = value[i] << shiftCount;
                }
                return Unsafe.As<ulong, Vector<ulong>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<nuint > ShiftLeft(in Vector<nuint > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftLeft(value, shiftCount);
#else
        switch(Vector<nuint>.Count)
        {
        case 1:
            return new(stackalloc []{
                value[0] << shiftCount,
            });
        case 2:
            return new(stackalloc []{
                value[0] << shiftCount,
                value[1] << shiftCount,
            });
        case 4:
            return new(stackalloc []{
                value[0] << shiftCount,
                value[1] << shiftCount,
                value[2] << shiftCount,
                value[3] << shiftCount,
            });
        case 8:
            return new(stackalloc []{
                value[0] << shiftCount,
                value[1] << shiftCount,
                value[2] << shiftCount,
                value[3] << shiftCount,
                value[4] << shiftCount,
                value[5] << shiftCount,
                value[6] << shiftCount,
                value[7] << shiftCount,
            });
        default:
            {
                var buffer = (stackalloc nuint[Vector<nuint>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = value[i] << shiftCount;
                }
                return Unsafe.As<nuint, Vector<nuint>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<sbyte > ShiftLeft(in Vector<sbyte > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftLeft(value, shiftCount);
#else
        switch(Vector<sbyte>.Count)
        {
        case 8:
            return new(stackalloc []{
                (sbyte)(value[0] << shiftCount),
                (sbyte)(value[1] << shiftCount),
                (sbyte)(value[2] << shiftCount),
                (sbyte)(value[3] << shiftCount),
                (sbyte)(value[4] << shiftCount),
                (sbyte)(value[5] << shiftCount),
                (sbyte)(value[6] << shiftCount),
                (sbyte)(value[7] << shiftCount),
            });
        case 16:
            return new(stackalloc []{
                (sbyte)(value[0] << shiftCount),
                (sbyte)(value[1] << shiftCount),
                (sbyte)(value[2] << shiftCount),
                (sbyte)(value[3] << shiftCount),
                (sbyte)(value[4] << shiftCount),
                (sbyte)(value[5] << shiftCount),
                (sbyte)(value[6] << shiftCount),
                (sbyte)(value[7] << shiftCount),
                (sbyte)(value[8] << shiftCount),
                (sbyte)(value[9] << shiftCount),
                (sbyte)(value[10] << shiftCount),
                (sbyte)(value[11] << shiftCount),
                (sbyte)(value[12] << shiftCount),
                (sbyte)(value[13] << shiftCount),
                (sbyte)(value[14] << shiftCount),
                (sbyte)(value[15] << shiftCount),
            });
        case 32:
            return new(stackalloc []{
                (sbyte)(value[0] << shiftCount),
                (sbyte)(value[1] << shiftCount),
                (sbyte)(value[2] << shiftCount),
                (sbyte)(value[3] << shiftCount),
                (sbyte)(value[4] << shiftCount),
                (sbyte)(value[5] << shiftCount),
                (sbyte)(value[6] << shiftCount),
                (sbyte)(value[7] << shiftCount),
                (sbyte)(value[8] << shiftCount),
                (sbyte)(value[9] << shiftCount),
                (sbyte)(value[10] << shiftCount),
                (sbyte)(value[11] << shiftCount),
                (sbyte)(value[12] << shiftCount),
                (sbyte)(value[13] << shiftCount),
                (sbyte)(value[14] << shiftCount),
                (sbyte)(value[15] << shiftCount),
                (sbyte)(value[16] << shiftCount),
                (sbyte)(value[17] << shiftCount),
                (sbyte)(value[18] << shiftCount),
                (sbyte)(value[19] << shiftCount),
                (sbyte)(value[20] << shiftCount),
                (sbyte)(value[21] << shiftCount),
                (sbyte)(value[22] << shiftCount),
                (sbyte)(value[23] << shiftCount),
                (sbyte)(value[24] << shiftCount),
                (sbyte)(value[25] << shiftCount),
                (sbyte)(value[26] << shiftCount),
                (sbyte)(value[27] << shiftCount),
                (sbyte)(value[28] << shiftCount),
                (sbyte)(value[29] << shiftCount),
                (sbyte)(value[30] << shiftCount),
                (sbyte)(value[31] << shiftCount),
            });
        default:
            {
                var buffer = (stackalloc sbyte[Vector<sbyte>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = (sbyte)(value[i] << shiftCount);
                }
                return Unsafe.As<sbyte, Vector<sbyte>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<short > ShiftLeft(in Vector<short > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftLeft(value, shiftCount);
#else
        switch(Vector<short>.Count)
        {
        case 4:
            return new(stackalloc []{
                (short)(value[0] << shiftCount),
                (short)(value[1] << shiftCount),
                (short)(value[2] << shiftCount),
                (short)(value[3] << shiftCount),
            });
        case 8:
            return new(stackalloc []{
                (short)(value[0] << shiftCount),
                (short)(value[1] << shiftCount),
                (short)(value[2] << shiftCount),
                (short)(value[3] << shiftCount),
                (short)(value[4] << shiftCount),
                (short)(value[5] << shiftCount),
                (short)(value[6] << shiftCount),
                (short)(value[7] << shiftCount),
            });
        case 16:
            return new(stackalloc []{
                (short)(value[0] << shiftCount),
                (short)(value[1] << shiftCount),
                (short)(value[2] << shiftCount),
                (short)(value[3] << shiftCount),
                (short)(value[4] << shiftCount),
                (short)(value[5] << shiftCount),
                (short)(value[6] << shiftCount),
                (short)(value[7] << shiftCount),
                (short)(value[8] << shiftCount),
                (short)(value[9] << shiftCount),
                (short)(value[10] << shiftCount),
                (short)(value[11] << shiftCount),
                (short)(value[12] << shiftCount),
                (short)(value[13] << shiftCount),
                (short)(value[14] << shiftCount),
                (short)(value[15] << shiftCount),
            });
        default:
            {
                var buffer = (stackalloc short[Vector<short>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = (short)(value[i] << shiftCount);
                }
                return Unsafe.As<short, Vector<short>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<int   > ShiftLeft(in Vector<int   > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftLeft(value, shiftCount);
#else
        switch(Vector<int>.Count)
        {
        case 2:
            return new(stackalloc []{
                value[0] << shiftCount,
                value[1] << shiftCount,
            });
        case 4:
            return new(stackalloc []{
                value[0] << shiftCount,
                value[1] << shiftCount,
                value[2] << shiftCount,
                value[3] << shiftCount,
            });
        case 8:
            return new(stackalloc []{
                value[0] << shiftCount,
                value[1] << shiftCount,
                value[2] << shiftCount,
                value[3] << shiftCount,
                value[4] << shiftCount,
                value[5] << shiftCount,
                value[6] << shiftCount,
                value[7] << shiftCount,
            });
        default:
            {
                var buffer = (stackalloc int[Vector<int>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = value[i] << shiftCount;
                }
                return Unsafe.As<int, Vector<int>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<long  > ShiftLeft(in Vector<long  > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftLeft(value, shiftCount);
#else
        switch(Vector<long>.Count)
        {
        case 1:
            return new(stackalloc []{
                value[0] << shiftCount,
            });
        case 2:
            return new(stackalloc []{
                value[0] << shiftCount,
                value[1] << shiftCount,
            });
        case 4:
            return new(stackalloc []{
                value[0] << shiftCount,
                value[1] << shiftCount,
                value[2] << shiftCount,
                value[3] << shiftCount,
            });
        default:
            {
                var buffer = (stackalloc long[Vector<long>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = value[i] << shiftCount;
                }
                return Unsafe.As<long, Vector<long>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<nint  > ShiftLeft(in Vector<nint  > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftLeft(value, shiftCount);
#else
        switch(Vector<nint>.Count)
        {
        case 1:
            return new(stackalloc []{
                value[0] << shiftCount,
            });
        case 2:
            return new(stackalloc []{
                value[0] << shiftCount,
                value[1] << shiftCount,
            });
        case 4:
            return new(stackalloc []{
                value[0] << shiftCount,
                value[1] << shiftCount,
                value[2] << shiftCount,
                value[3] << shiftCount,
            });
        case 8:
            return new(stackalloc []{
                value[0] << shiftCount,
                value[1] << shiftCount,
                value[2] << shiftCount,
                value[3] << shiftCount,
                value[4] << shiftCount,
                value[5] << shiftCount,
                value[6] << shiftCount,
                value[7] << shiftCount,
            });
        default:
            {
                var buffer = (stackalloc nint[Vector<nint>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = value[i] << shiftCount;
                }
                return Unsafe.As<nint, Vector<nint>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<byte  > ShiftRightLogical(in Vector<byte  > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftRightLogical(value, shiftCount);
#else
        switch(Vector<byte>.Count)
        {
        case 8:
            return new(stackalloc []{
                (byte  )(value[0] >> shiftCount),
                (byte  )(value[1] >> shiftCount),
                (byte  )(value[2] >> shiftCount),
                (byte  )(value[3] >> shiftCount),
                (byte  )(value[4] >> shiftCount),
                (byte  )(value[5] >> shiftCount),
                (byte  )(value[6] >> shiftCount),
                (byte  )(value[7] >> shiftCount),
            });
        case 16:
            return new(stackalloc []{
                (byte  )(value[0] >> shiftCount),
                (byte  )(value[1] >> shiftCount),
                (byte  )(value[2] >> shiftCount),
                (byte  )(value[3] >> shiftCount),
                (byte  )(value[4] >> shiftCount),
                (byte  )(value[5] >> shiftCount),
                (byte  )(value[6] >> shiftCount),
                (byte  )(value[7] >> shiftCount),
                (byte  )(value[8] >> shiftCount),
                (byte  )(value[9] >> shiftCount),
                (byte  )(value[10] >> shiftCount),
                (byte  )(value[11] >> shiftCount),
                (byte  )(value[12] >> shiftCount),
                (byte  )(value[13] >> shiftCount),
                (byte  )(value[14] >> shiftCount),
                (byte  )(value[15] >> shiftCount),
            });
        case 32:
            return new(stackalloc []{
                (byte  )(value[0] >> shiftCount),
                (byte  )(value[1] >> shiftCount),
                (byte  )(value[2] >> shiftCount),
                (byte  )(value[3] >> shiftCount),
                (byte  )(value[4] >> shiftCount),
                (byte  )(value[5] >> shiftCount),
                (byte  )(value[6] >> shiftCount),
                (byte  )(value[7] >> shiftCount),
                (byte  )(value[8] >> shiftCount),
                (byte  )(value[9] >> shiftCount),
                (byte  )(value[10] >> shiftCount),
                (byte  )(value[11] >> shiftCount),
                (byte  )(value[12] >> shiftCount),
                (byte  )(value[13] >> shiftCount),
                (byte  )(value[14] >> shiftCount),
                (byte  )(value[15] >> shiftCount),
                (byte  )(value[16] >> shiftCount),
                (byte  )(value[17] >> shiftCount),
                (byte  )(value[18] >> shiftCount),
                (byte  )(value[19] >> shiftCount),
                (byte  )(value[20] >> shiftCount),
                (byte  )(value[21] >> shiftCount),
                (byte  )(value[22] >> shiftCount),
                (byte  )(value[23] >> shiftCount),
                (byte  )(value[24] >> shiftCount),
                (byte  )(value[25] >> shiftCount),
                (byte  )(value[26] >> shiftCount),
                (byte  )(value[27] >> shiftCount),
                (byte  )(value[28] >> shiftCount),
                (byte  )(value[29] >> shiftCount),
                (byte  )(value[30] >> shiftCount),
                (byte  )(value[31] >> shiftCount),
            });
        default:
            {
                var buffer = (stackalloc byte[Vector<byte>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = (byte  )(value[i] >> shiftCount);
                }
                return Unsafe.As<byte, Vector<byte>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<ushort> ShiftRightLogical(in Vector<ushort> value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftRightLogical(value, shiftCount);
#else
        switch(Vector<ushort>.Count)
        {
        case 4:
            return new(stackalloc []{
                (ushort)(value[0] >> shiftCount),
                (ushort)(value[1] >> shiftCount),
                (ushort)(value[2] >> shiftCount),
                (ushort)(value[3] >> shiftCount),
            });
        case 8:
            return new(stackalloc []{
                (ushort)(value[0] >> shiftCount),
                (ushort)(value[1] >> shiftCount),
                (ushort)(value[2] >> shiftCount),
                (ushort)(value[3] >> shiftCount),
                (ushort)(value[4] >> shiftCount),
                (ushort)(value[5] >> shiftCount),
                (ushort)(value[6] >> shiftCount),
                (ushort)(value[7] >> shiftCount),
            });
        case 16:
            return new(stackalloc []{
                (ushort)(value[0] >> shiftCount),
                (ushort)(value[1] >> shiftCount),
                (ushort)(value[2] >> shiftCount),
                (ushort)(value[3] >> shiftCount),
                (ushort)(value[4] >> shiftCount),
                (ushort)(value[5] >> shiftCount),
                (ushort)(value[6] >> shiftCount),
                (ushort)(value[7] >> shiftCount),
                (ushort)(value[8] >> shiftCount),
                (ushort)(value[9] >> shiftCount),
                (ushort)(value[10] >> shiftCount),
                (ushort)(value[11] >> shiftCount),
                (ushort)(value[12] >> shiftCount),
                (ushort)(value[13] >> shiftCount),
                (ushort)(value[14] >> shiftCount),
                (ushort)(value[15] >> shiftCount),
            });
        default:
            {
                var buffer = (stackalloc ushort[Vector<ushort>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = (ushort)(value[i] >> shiftCount);
                }
                return Unsafe.As<ushort, Vector<ushort>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<uint  > ShiftRightLogical(in Vector<uint  > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftRightLogical(value, shiftCount);
#else
        switch(Vector<uint>.Count)
        {
        case 2:
            return new(stackalloc []{
                (uint  )(value[0] >> shiftCount),
                (uint  )(value[1] >> shiftCount),
            });
        case 4:
            return new(stackalloc []{
                (uint  )(value[0] >> shiftCount),
                (uint  )(value[1] >> shiftCount),
                (uint  )(value[2] >> shiftCount),
                (uint  )(value[3] >> shiftCount),
            });
        case 8:
            return new(stackalloc []{
                (uint  )(value[0] >> shiftCount),
                (uint  )(value[1] >> shiftCount),
                (uint  )(value[2] >> shiftCount),
                (uint  )(value[3] >> shiftCount),
                (uint  )(value[4] >> shiftCount),
                (uint  )(value[5] >> shiftCount),
                (uint  )(value[6] >> shiftCount),
                (uint  )(value[7] >> shiftCount),
            });
        default:
            {
                var buffer = (stackalloc uint[Vector<uint>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = (uint  )(value[i] >> shiftCount);
                }
                return Unsafe.As<uint, Vector<uint>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<ulong > ShiftRightLogical(in Vector<ulong > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftRightLogical(value, shiftCount);
#else
        switch(Vector<ulong>.Count)
        {
        case 1:
            return new(stackalloc []{
                (ulong )(value[0] >> shiftCount),
            });
        case 2:
            return new(stackalloc []{
                (ulong )(value[0] >> shiftCount),
                (ulong )(value[1] >> shiftCount),
            });
        case 4:
            return new(stackalloc []{
                (ulong )(value[0] >> shiftCount),
                (ulong )(value[1] >> shiftCount),
                (ulong )(value[2] >> shiftCount),
                (ulong )(value[3] >> shiftCount),
            });
        default:
            {
                var buffer = (stackalloc ulong[Vector<ulong>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = (ulong )(value[i] >> shiftCount);
                }
                return Unsafe.As<ulong, Vector<ulong>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<nuint > ShiftRightLogical(in Vector<nuint > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftRightLogical(value, shiftCount);
#else
        switch(Vector<nuint>.Count)
        {
        case 1:
            return new(stackalloc []{
                (nuint )(value[0] >> shiftCount),
            });
        case 2:
            return new(stackalloc []{
                (nuint )(value[0] >> shiftCount),
                (nuint )(value[1] >> shiftCount),
            });
        case 4:
            return new(stackalloc []{
                (nuint )(value[0] >> shiftCount),
                (nuint )(value[1] >> shiftCount),
                (nuint )(value[2] >> shiftCount),
                (nuint )(value[3] >> shiftCount),
            });
        case 8:
            return new(stackalloc []{
                (nuint )(value[0] >> shiftCount),
                (nuint )(value[1] >> shiftCount),
                (nuint )(value[2] >> shiftCount),
                (nuint )(value[3] >> shiftCount),
                (nuint )(value[4] >> shiftCount),
                (nuint )(value[5] >> shiftCount),
                (nuint )(value[6] >> shiftCount),
                (nuint )(value[7] >> shiftCount),
            });
        default:
            {
                var buffer = (stackalloc nuint[Vector<nuint>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = (nuint )(value[i] >> shiftCount);
                }
                return Unsafe.As<nuint, Vector<nuint>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<sbyte > ShiftRightLogical(in Vector<sbyte > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftRightLogical(value, shiftCount);
#else
        switch(Vector<sbyte>.Count)
        {
        case 8:
            return new(stackalloc []{
                (sbyte )((byte  )value[0] >> shiftCount),
                (sbyte )((byte  )value[1] >> shiftCount),
                (sbyte )((byte  )value[2] >> shiftCount),
                (sbyte )((byte  )value[3] >> shiftCount),
                (sbyte )((byte  )value[4] >> shiftCount),
                (sbyte )((byte  )value[5] >> shiftCount),
                (sbyte )((byte  )value[6] >> shiftCount),
                (sbyte )((byte  )value[7] >> shiftCount),
            });
        case 16:
            return new(stackalloc []{
                (sbyte )((byte  )value[0] >> shiftCount),
                (sbyte )((byte  )value[1] >> shiftCount),
                (sbyte )((byte  )value[2] >> shiftCount),
                (sbyte )((byte  )value[3] >> shiftCount),
                (sbyte )((byte  )value[4] >> shiftCount),
                (sbyte )((byte  )value[5] >> shiftCount),
                (sbyte )((byte  )value[6] >> shiftCount),
                (sbyte )((byte  )value[7] >> shiftCount),
                (sbyte )((byte  )value[8] >> shiftCount),
                (sbyte )((byte  )value[9] >> shiftCount),
                (sbyte )((byte  )value[10] >> shiftCount),
                (sbyte )((byte  )value[11] >> shiftCount),
                (sbyte )((byte  )value[12] >> shiftCount),
                (sbyte )((byte  )value[13] >> shiftCount),
                (sbyte )((byte  )value[14] >> shiftCount),
                (sbyte )((byte  )value[15] >> shiftCount),
            });
        case 32:
            return new(stackalloc []{
                (sbyte )((byte  )value[0] >> shiftCount),
                (sbyte )((byte  )value[1] >> shiftCount),
                (sbyte )((byte  )value[2] >> shiftCount),
                (sbyte )((byte  )value[3] >> shiftCount),
                (sbyte )((byte  )value[4] >> shiftCount),
                (sbyte )((byte  )value[5] >> shiftCount),
                (sbyte )((byte  )value[6] >> shiftCount),
                (sbyte )((byte  )value[7] >> shiftCount),
                (sbyte )((byte  )value[8] >> shiftCount),
                (sbyte )((byte  )value[9] >> shiftCount),
                (sbyte )((byte  )value[10] >> shiftCount),
                (sbyte )((byte  )value[11] >> shiftCount),
                (sbyte )((byte  )value[12] >> shiftCount),
                (sbyte )((byte  )value[13] >> shiftCount),
                (sbyte )((byte  )value[14] >> shiftCount),
                (sbyte )((byte  )value[15] >> shiftCount),
                (sbyte )((byte  )value[16] >> shiftCount),
                (sbyte )((byte  )value[17] >> shiftCount),
                (sbyte )((byte  )value[18] >> shiftCount),
                (sbyte )((byte  )value[19] >> shiftCount),
                (sbyte )((byte  )value[20] >> shiftCount),
                (sbyte )((byte  )value[21] >> shiftCount),
                (sbyte )((byte  )value[22] >> shiftCount),
                (sbyte )((byte  )value[23] >> shiftCount),
                (sbyte )((byte  )value[24] >> shiftCount),
                (sbyte )((byte  )value[25] >> shiftCount),
                (sbyte )((byte  )value[26] >> shiftCount),
                (sbyte )((byte  )value[27] >> shiftCount),
                (sbyte )((byte  )value[28] >> shiftCount),
                (sbyte )((byte  )value[29] >> shiftCount),
                (sbyte )((byte  )value[30] >> shiftCount),
                (sbyte )((byte  )value[31] >> shiftCount),
            });
        default:
            {
                var buffer = (stackalloc sbyte[Vector<sbyte>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = (sbyte )((byte  )value[i] >> shiftCount);
                }
                return Unsafe.As<sbyte, Vector<sbyte>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<short > ShiftRightLogical(in Vector<short > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftRightLogical(value, shiftCount);
#else
        switch(Vector<short>.Count)
        {
        case 4:
            return new(stackalloc []{
                (short )((ushort)value[0] >> shiftCount),
                (short )((ushort)value[1] >> shiftCount),
                (short )((ushort)value[2] >> shiftCount),
                (short )((ushort)value[3] >> shiftCount),
            });
        case 8:
            return new(stackalloc []{
                (short )((ushort)value[0] >> shiftCount),
                (short )((ushort)value[1] >> shiftCount),
                (short )((ushort)value[2] >> shiftCount),
                (short )((ushort)value[3] >> shiftCount),
                (short )((ushort)value[4] >> shiftCount),
                (short )((ushort)value[5] >> shiftCount),
                (short )((ushort)value[6] >> shiftCount),
                (short )((ushort)value[7] >> shiftCount),
            });
        case 16:
            return new(stackalloc []{
                (short )((ushort)value[0] >> shiftCount),
                (short )((ushort)value[1] >> shiftCount),
                (short )((ushort)value[2] >> shiftCount),
                (short )((ushort)value[3] >> shiftCount),
                (short )((ushort)value[4] >> shiftCount),
                (short )((ushort)value[5] >> shiftCount),
                (short )((ushort)value[6] >> shiftCount),
                (short )((ushort)value[7] >> shiftCount),
                (short )((ushort)value[8] >> shiftCount),
                (short )((ushort)value[9] >> shiftCount),
                (short )((ushort)value[10] >> shiftCount),
                (short )((ushort)value[11] >> shiftCount),
                (short )((ushort)value[12] >> shiftCount),
                (short )((ushort)value[13] >> shiftCount),
                (short )((ushort)value[14] >> shiftCount),
                (short )((ushort)value[15] >> shiftCount),
            });
        default:
            {
                var buffer = (stackalloc short[Vector<short>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = (short )((ushort)value[i] >> shiftCount);
                }
                return Unsafe.As<short, Vector<short>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<int   > ShiftRightLogical(in Vector<int   > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftRightLogical(value, shiftCount);
#else
        switch(Vector<int>.Count)
        {
        case 2:
            return new(stackalloc []{
                (int   )((uint  )value[0] >> shiftCount),
                (int   )((uint  )value[1] >> shiftCount),
            });
        case 4:
            return new(stackalloc []{
                (int   )((uint  )value[0] >> shiftCount),
                (int   )((uint  )value[1] >> shiftCount),
                (int   )((uint  )value[2] >> shiftCount),
                (int   )((uint  )value[3] >> shiftCount),
            });
        case 8:
            return new(stackalloc []{
                (int   )((uint  )value[0] >> shiftCount),
                (int   )((uint  )value[1] >> shiftCount),
                (int   )((uint  )value[2] >> shiftCount),
                (int   )((uint  )value[3] >> shiftCount),
                (int   )((uint  )value[4] >> shiftCount),
                (int   )((uint  )value[5] >> shiftCount),
                (int   )((uint  )value[6] >> shiftCount),
                (int   )((uint  )value[7] >> shiftCount),
            });
        default:
            {
                var buffer = (stackalloc int[Vector<int>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = (int   )((uint  )value[i] >> shiftCount);
                }
                return Unsafe.As<int, Vector<int>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<long  > ShiftRightLogical(in Vector<long  > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftRightLogical(value, shiftCount);
#else
        switch(Vector<long>.Count)
        {
        case 1:
            return new(stackalloc []{
                (long  )((ulong )value[0] >> shiftCount),
            });
        case 2:
            return new(stackalloc []{
                (long  )((ulong )value[0] >> shiftCount),
                (long  )((ulong )value[1] >> shiftCount),
            });
        case 4:
            return new(stackalloc []{
                (long  )((ulong )value[0] >> shiftCount),
                (long  )((ulong )value[1] >> shiftCount),
                (long  )((ulong )value[2] >> shiftCount),
                (long  )((ulong )value[3] >> shiftCount),
            });
        default:
            {
                var buffer = (stackalloc long[Vector<long>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = (long  )((ulong )value[i] >> shiftCount);
                }
                return Unsafe.As<long, Vector<long>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<nint  > ShiftRightLogical(in Vector<nint  > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftRightLogical(value, shiftCount);
#else
        switch(Vector<nint>.Count)
        {
        case 1:
            return new(stackalloc []{
                (nint  )((nuint )value[0] >> shiftCount),
            });
        case 2:
            return new(stackalloc []{
                (nint  )((nuint )value[0] >> shiftCount),
                (nint  )((nuint )value[1] >> shiftCount),
            });
        case 4:
            return new(stackalloc []{
                (nint  )((nuint )value[0] >> shiftCount),
                (nint  )((nuint )value[1] >> shiftCount),
                (nint  )((nuint )value[2] >> shiftCount),
                (nint  )((nuint )value[3] >> shiftCount),
            });
        case 8:
            return new(stackalloc []{
                (nint  )((nuint )value[0] >> shiftCount),
                (nint  )((nuint )value[1] >> shiftCount),
                (nint  )((nuint )value[2] >> shiftCount),
                (nint  )((nuint )value[3] >> shiftCount),
                (nint  )((nuint )value[4] >> shiftCount),
                (nint  )((nuint )value[5] >> shiftCount),
                (nint  )((nuint )value[6] >> shiftCount),
                (nint  )((nuint )value[7] >> shiftCount),
            });
        default:
            {
                var buffer = (stackalloc nint[Vector<nint>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = (nint  )((nuint )value[i] >> shiftCount);
                }
                return Unsafe.As<nint, Vector<nint>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<sbyte > ShiftRightArithmetic(in Vector<sbyte > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftRightArithmetic(value, shiftCount);
#else
        switch(Vector<sbyte>.Count)
        {
        case 8:
            return new(stackalloc []{
                (sbyte)(value[0] >> shiftCount),
                (sbyte)(value[1] >> shiftCount),
                (sbyte)(value[2] >> shiftCount),
                (sbyte)(value[3] >> shiftCount),
                (sbyte)(value[4] >> shiftCount),
                (sbyte)(value[5] >> shiftCount),
                (sbyte)(value[6] >> shiftCount),
                (sbyte)(value[7] >> shiftCount),
            });
        case 16:
            return new(stackalloc []{
                (sbyte)(value[0] >> shiftCount),
                (sbyte)(value[1] >> shiftCount),
                (sbyte)(value[2] >> shiftCount),
                (sbyte)(value[3] >> shiftCount),
                (sbyte)(value[4] >> shiftCount),
                (sbyte)(value[5] >> shiftCount),
                (sbyte)(value[6] >> shiftCount),
                (sbyte)(value[7] >> shiftCount),
                (sbyte)(value[8] >> shiftCount),
                (sbyte)(value[9] >> shiftCount),
                (sbyte)(value[10] >> shiftCount),
                (sbyte)(value[11] >> shiftCount),
                (sbyte)(value[12] >> shiftCount),
                (sbyte)(value[13] >> shiftCount),
                (sbyte)(value[14] >> shiftCount),
                (sbyte)(value[15] >> shiftCount),
            });
        case 32:
            return new(stackalloc []{
                (sbyte)(value[0] >> shiftCount),
                (sbyte)(value[1] >> shiftCount),
                (sbyte)(value[2] >> shiftCount),
                (sbyte)(value[3] >> shiftCount),
                (sbyte)(value[4] >> shiftCount),
                (sbyte)(value[5] >> shiftCount),
                (sbyte)(value[6] >> shiftCount),
                (sbyte)(value[7] >> shiftCount),
                (sbyte)(value[8] >> shiftCount),
                (sbyte)(value[9] >> shiftCount),
                (sbyte)(value[10] >> shiftCount),
                (sbyte)(value[11] >> shiftCount),
                (sbyte)(value[12] >> shiftCount),
                (sbyte)(value[13] >> shiftCount),
                (sbyte)(value[14] >> shiftCount),
                (sbyte)(value[15] >> shiftCount),
                (sbyte)(value[16] >> shiftCount),
                (sbyte)(value[17] >> shiftCount),
                (sbyte)(value[18] >> shiftCount),
                (sbyte)(value[19] >> shiftCount),
                (sbyte)(value[20] >> shiftCount),
                (sbyte)(value[21] >> shiftCount),
                (sbyte)(value[22] >> shiftCount),
                (sbyte)(value[23] >> shiftCount),
                (sbyte)(value[24] >> shiftCount),
                (sbyte)(value[25] >> shiftCount),
                (sbyte)(value[26] >> shiftCount),
                (sbyte)(value[27] >> shiftCount),
                (sbyte)(value[28] >> shiftCount),
                (sbyte)(value[29] >> shiftCount),
                (sbyte)(value[30] >> shiftCount),
                (sbyte)(value[31] >> shiftCount),
            });
        default:
            {
                var buffer = (stackalloc sbyte[Vector<sbyte>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = (sbyte)(value[i] >> shiftCount);
                }
                return Unsafe.As<sbyte, Vector<sbyte>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<short > ShiftRightArithmetic(in Vector<short > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftRightArithmetic(value, shiftCount);
#else
        switch(Vector<short>.Count)
        {
        case 4:
            return new(stackalloc []{
                (short)(value[0] >> shiftCount),
                (short)(value[1] >> shiftCount),
                (short)(value[2] >> shiftCount),
                (short)(value[3] >> shiftCount),
            });
        case 8:
            return new(stackalloc []{
                (short)(value[0] >> shiftCount),
                (short)(value[1] >> shiftCount),
                (short)(value[2] >> shiftCount),
                (short)(value[3] >> shiftCount),
                (short)(value[4] >> shiftCount),
                (short)(value[5] >> shiftCount),
                (short)(value[6] >> shiftCount),
                (short)(value[7] >> shiftCount),
            });
        case 16:
            return new(stackalloc []{
                (short)(value[0] >> shiftCount),
                (short)(value[1] >> shiftCount),
                (short)(value[2] >> shiftCount),
                (short)(value[3] >> shiftCount),
                (short)(value[4] >> shiftCount),
                (short)(value[5] >> shiftCount),
                (short)(value[6] >> shiftCount),
                (short)(value[7] >> shiftCount),
                (short)(value[8] >> shiftCount),
                (short)(value[9] >> shiftCount),
                (short)(value[10] >> shiftCount),
                (short)(value[11] >> shiftCount),
                (short)(value[12] >> shiftCount),
                (short)(value[13] >> shiftCount),
                (short)(value[14] >> shiftCount),
                (short)(value[15] >> shiftCount),
            });
        default:
            {
                var buffer = (stackalloc short[Vector<short>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = (short)(value[i] >> shiftCount);
                }
                return Unsafe.As<short, Vector<short>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<int   > ShiftRightArithmetic(in Vector<int   > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftRightArithmetic(value, shiftCount);
#else
        switch(Vector<int>.Count)
        {
        case 2:
            return new(stackalloc []{
                (int  )(value[0] >> shiftCount),
                (int  )(value[1] >> shiftCount),
            });
        case 4:
            return new(stackalloc []{
                (int  )(value[0] >> shiftCount),
                (int  )(value[1] >> shiftCount),
                (int  )(value[2] >> shiftCount),
                (int  )(value[3] >> shiftCount),
            });
        case 8:
            return new(stackalloc []{
                (int  )(value[0] >> shiftCount),
                (int  )(value[1] >> shiftCount),
                (int  )(value[2] >> shiftCount),
                (int  )(value[3] >> shiftCount),
                (int  )(value[4] >> shiftCount),
                (int  )(value[5] >> shiftCount),
                (int  )(value[6] >> shiftCount),
                (int  )(value[7] >> shiftCount),
            });
        default:
            {
                var buffer = (stackalloc int[Vector<int>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = (int  )(value[i] >> shiftCount);
                }
                return Unsafe.As<int, Vector<int>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<long  > ShiftRightArithmetic(in Vector<long  > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftRightArithmetic(value, shiftCount);
#else
        switch(Vector<long>.Count)
        {
        case 1:
            return new(stackalloc []{
                (long )(value[0] >> shiftCount),
            });
        case 2:
            return new(stackalloc []{
                (long )(value[0] >> shiftCount),
                (long )(value[1] >> shiftCount),
            });
        case 4:
            return new(stackalloc []{
                (long )(value[0] >> shiftCount),
                (long )(value[1] >> shiftCount),
                (long )(value[2] >> shiftCount),
                (long )(value[3] >> shiftCount),
            });
        default:
            {
                var buffer = (stackalloc long[Vector<long>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = (long )(value[i] >> shiftCount);
                }
                return Unsafe.As<long, Vector<long>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<nint  > ShiftRightArithmetic(in Vector<nint  > value, int shiftCount)
    {
#if NET7_0_OR_GREATER
        return NVector.ShiftRightArithmetic(value, shiftCount);
#else
        switch(Vector<nint>.Count)
        {
        case 1:
            return new(stackalloc []{
                (nint )(value[0] >> shiftCount),
            });
        case 2:
            return new(stackalloc []{
                (nint )(value[0] >> shiftCount),
                (nint )(value[1] >> shiftCount),
            });
        case 4:
            return new(stackalloc []{
                (nint )(value[0] >> shiftCount),
                (nint )(value[1] >> shiftCount),
                (nint )(value[2] >> shiftCount),
                (nint )(value[3] >> shiftCount),
            });
        case 8:
            return new(stackalloc []{
                (nint )(value[0] >> shiftCount),
                (nint )(value[1] >> shiftCount),
                (nint )(value[2] >> shiftCount),
                (nint )(value[3] >> shiftCount),
                (nint )(value[4] >> shiftCount),
                (nint )(value[5] >> shiftCount),
                (nint )(value[6] >> shiftCount),
                (nint )(value[7] >> shiftCount),
            });
        default:
            {
                var buffer = (stackalloc nint[Vector<nint>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = (nint )(value[i] >> shiftCount);
                }
                return Unsafe.As<nint, Vector<nint>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<float > Ceiling(in Vector<float > value)
    {
#if NET6_0_OR_GREATER
        return NVector.Ceiling(value);
#else
        switch(Vector<float>.Count)
        {
        case 2:
            return new(stackalloc []{
                ValueOperation.Ceiling(value[0]),
                ValueOperation.Ceiling(value[1]),
            });
        case 4:
            return new(stackalloc []{
                ValueOperation.Ceiling(value[0]),
                ValueOperation.Ceiling(value[1]),
                ValueOperation.Ceiling(value[2]),
                ValueOperation.Ceiling(value[3]),
            });
        case 8:
            return new(stackalloc []{
                ValueOperation.Ceiling(value[0]),
                ValueOperation.Ceiling(value[1]),
                ValueOperation.Ceiling(value[2]),
                ValueOperation.Ceiling(value[3]),
                ValueOperation.Ceiling(value[4]),
                ValueOperation.Ceiling(value[5]),
                ValueOperation.Ceiling(value[6]),
                ValueOperation.Ceiling(value[7]),
            });
        default:
            {
                var buffer = (stackalloc float[Vector<float>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = ValueOperation.Ceiling(value[i]);
                }
                return Unsafe.As<float, Vector<float>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<double> Ceiling(in Vector<double> value)
    {
#if NET6_0_OR_GREATER
        return NVector.Ceiling(value);
#else
        switch(Vector<double>.Count)
        {
        case 1:
            return new(stackalloc []{
                ValueOperation.Ceiling(value[0]),
            });
        case 2:
            return new(stackalloc []{
                ValueOperation.Ceiling(value[0]),
                ValueOperation.Ceiling(value[1]),
            });
        case 4:
            return new(stackalloc []{
                ValueOperation.Ceiling(value[0]),
                ValueOperation.Ceiling(value[1]),
                ValueOperation.Ceiling(value[2]),
                ValueOperation.Ceiling(value[3]),
            });
        default:
            {
                var buffer = (stackalloc double[Vector<double>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = ValueOperation.Ceiling(value[i]);
                }
                return Unsafe.As<double, Vector<double>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<float > Floor  (in Vector<float > value)
    {
#if NET6_0_OR_GREATER
        return NVector.Floor  (value);
#else
        switch(Vector<float>.Count)
        {
        case 2:
            return new(stackalloc []{
                ValueOperation.Floor  (value[0]),
                ValueOperation.Floor  (value[1]),
            });
        case 4:
            return new(stackalloc []{
                ValueOperation.Floor  (value[0]),
                ValueOperation.Floor  (value[1]),
                ValueOperation.Floor  (value[2]),
                ValueOperation.Floor  (value[3]),
            });
        case 8:
            return new(stackalloc []{
                ValueOperation.Floor  (value[0]),
                ValueOperation.Floor  (value[1]),
                ValueOperation.Floor  (value[2]),
                ValueOperation.Floor  (value[3]),
                ValueOperation.Floor  (value[4]),
                ValueOperation.Floor  (value[5]),
                ValueOperation.Floor  (value[6]),
                ValueOperation.Floor  (value[7]),
            });
        default:
            {
                var buffer = (stackalloc float[Vector<float>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = ValueOperation.Floor  (value[i]);
                }
                return Unsafe.As<float, Vector<float>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }


    public static partial Vector<double> Floor  (in Vector<double> value)
    {
#if NET6_0_OR_GREATER
        return NVector.Floor  (value);
#else
        switch(Vector<double>.Count)
        {
        case 1:
            return new(stackalloc []{
                ValueOperation.Floor  (value[0]),
            });
        case 2:
            return new(stackalloc []{
                ValueOperation.Floor  (value[0]),
                ValueOperation.Floor  (value[1]),
            });
        case 4:
            return new(stackalloc []{
                ValueOperation.Floor  (value[0]),
                ValueOperation.Floor  (value[1]),
                ValueOperation.Floor  (value[2]),
                ValueOperation.Floor  (value[3]),
            });
        default:
            {
                var buffer = (stackalloc double[Vector<double>.Count]);
                for(var i = 0; i < buffer.Length; ++i)
                {
                    buffer[i] = ValueOperation.Floor  (value[i]);
                }
                return Unsafe.As<double, Vector<double>>(ref buffer[0]);
            }
        }
        throw new NotImplementedException();
#endif
    }

}
