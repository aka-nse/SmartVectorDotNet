name: .NET Core CI

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

jobs:
  build-and-test:
    runs-on: [windows-latest]

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Restore dependencies
      run: dotnet restore src/SmartVectorDotNet.sln

    - name: Build solution
      run: dotnet build src/SmartVectorDotNet.sln --no-restore

    - name: Run tests
      run: dotnet test src/SmartVectorDotNet.sln --no-build --verbosity normal
